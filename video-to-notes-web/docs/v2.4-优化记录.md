# v2.4 优化记录 — 视频解析层统一重构

> **分支**: `feat/video-parser-improvements`
> **日期**: 2026-07-09
> **基于**: main (160a3ce)

---

## 一、改动概述

将 `app.py` 中三个硬编码 if-elif 分支的管道函数（`step_search` / `step_resolve` / `step_download`）重构为**可扩展的统一解析器架构**。yt-dlp 作为主力引擎覆盖 1800+ 平台，小红书保留 xhs CLI 专用通道。

### 量化指标

| 指标 | 重构前 | 重构后 |
|------|:--:|:--:|
| app.py 行数 | ~1030 | ~1170 |
| 管道函数行数 | ~180 | ~55 |
| 显式支持平台 | 3 | 9 |
| 兜底覆盖 | 无 | 1800+（yt-dlp unknown） |
| 加一个新平台 | 改 3 处函数 | 1 行注册 |

---

## 二、新增代码

### BaseResolver 抽象基类

```python
class BaseResolver:
    platform_id: str = ""
    def search(self, query, task) -> list[dict]: ...
    def resolve(self, url, task, **kwargs) -> dict: ...
    def download(self, meta, output_path, task) -> bool: ...
```

### YtDlpResolver（主力解析器）

- `search()` — `yt-dlp --flat-playlist --dump-json "ytsearch8:{query}"`
- `resolve()` — `yt-dlp --dump-json` → `_map_meta()` 统一字段映射
- `download()` — `yt-dlp -x --audio-format mp3`
- `_cookie_flags()` — B站/抖音先带 Chrome Cookie 再裸连
- `_map_meta()` — yt-dlp 原始字段 → 标准 Meta 字典

### XhsResolver（小红书专用）

将原有 xhs 分支逻辑完整搬入，保留：
- 短链追踪（xhslink → 提取 note_id + xsec_token）
- 新旧 xhs-cli 响应格式兼容（`data.items[0].note_card` / `data.*` flat）
- CDN 直链提取（`video.media.stream` → `masterUrl`）
- curl + ffmpeg 下载

### 平台注册表

```python
PLATFORM_REGISTRY = [
    (r'(xhs|xiaohongshu|xhslink)\.com',   XhsResolver()),
    (r'bilibili\.com|b23\.tv',           YtDlpResolver("bilibili", cookie_required=True)),
    (r'youtube\.com|youtu\.be',          YtDlpResolver("youtube")),
    (r'douyin\.com',                     YtDlpResolver("douyin", cookie_required=True)),
    (r'kuaishou\.com',                   YtDlpResolver("kuaishou")),
    (r'(youku|yk)\.com',                 YtDlpResolver("youku")),
    (r'mgtv\.com',                       YtDlpResolver("mgtv")),
    (r'v\.qq\.com',                      YtDlpResolver("qqvideo")),
    (r'iqiyi\.com',                      YtDlpResolver("iqiyi")),
]

detect_platform(url) → BaseResolver  # URL 域名自动匹配
```

---

## 三、删除代码

| 删除项 | 原因 |
|--------|------|
| `PROXY = "http://127.0.0.1:7890"` | 死代码，从未使用 |
| `_get_config()` 函数 | 死代码，无人调用 |
| `step_search` 中 3 个 if-elif 分支（~65行） | 由 PLATFORM_MAP 委托替代 |
| `step_resolve` 中 3 个 if-elif 分支（~70行） | 由 detect_platform() + resolver.resolve() 替代 |
| `step_download` 中 3 个 if-elif 分支（~25行） | 由 resolver.download() 替代 |

---

## 四、Bug 修复

### Bug #1: 环境变量命名冲突导致 LLM 调用失败

**严重程度**: 🔴 高

**现象**: 在 Claude Code 环境中启动 app 时，笔记生成步骤走兜底函数（`_basic_notes`），只输出原始转录文本，缺少 AI 结构化内容。但通过 `start.sh` 或 macOS `.app` 启动时正常。

**根因**: `_read_api_config()` 使用 `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_BASE_URL` / `ANTHROPIC_MODEL` 环境变量，与 Claude Code 自身的环境变量**命名完全相同**。Claude Code 设的值（`https://api.deepseek.com/anthropic`、`DeepSeek-V4-pro[1m]`）不符合 app 的 OpenAI 兼容 API 请求格式，`_call_llm()` 失败 → 走兜底函数。

**修复**: 环境变量名改为项目专属：
```
ANTHROPIC_AUTH_TOKEN  → VTN_API_KEY
ANTHROPIC_BASE_URL    → VTN_API_BASE
ANTHROPIC_MODEL       → VTN_MODEL
```

**影响范围**: 仅 `_read_api_config()`（第 497 行）。settings.json 配置路径不受影响。如果之前有人手动设过旧的 `ANTHROPIC_*` 环境变量来覆盖配置，需要改用新名称。

**Commit**: `17d8a1c`

---

### Bug #2: 兜底函数文案过时

**现象**: `_basic_notes()` 输出的笔记标注「转录：Groq Whisper large-v3」，实际已改用本地 Whisper tiny。

**修复**: 文案改为「转录：本地 Whisper tiny」。

---

### Bug #3: yt-dlp 简介字段显示 "-"

**现象**: B站视频的 `description` 字段显示 `-` 占位符。

**修复**: `_map_meta()` 中对空值和 `"-"` 统一置为空字符串。

---

## 五、平台测试结果

| # | 平台 | 解析器 | 结果 | 备注 |
|:--:|------|------|:--:|------|
| 1 | 小红书 | XhsResolver | ✅ | 短链追踪、新旧格式兼容、CDN 直链 |
| 2 | B站 | YtDlpResolver | ✅ | Chrome Cookie 降级链正常 |
| 3 | YouTube | YtDlpResolver | ✅ | 需要代理（当前 7892 端口） |
| 4 | 抖音 | YtDlpResolver | ❌ | yt-dlp extractor 已挂 → TODO O |
| 5 | 快手 | YtDlpResolver | ❌ | yt-dlp extractor 已挂 → TODO O |
| 6-9 | 优酷/芒果/腾讯/爱奇艺 | YtDlpResolver | ⏸ | 未测，保留在注册表 |

### 端到端测试（小红书）

```
输入: http://xhslink.com/o/OTSu6aXDFP
模式: standard, mermaid=false

✅ resolve:     OPC是什么？普通人怎么用AI搞💰？ (Chris AI＆life, 4664赞)
✅ download:    3366 KB 音频
✅ transcribe:  1239 字转录
✅ generate:    2259 字结构化笔记

输出: 核心论点 + 内容框架(4个子节) + 关键概念 + 个人思考
```

---

## 六、关联文件

| 文件 | 改动 |
|------|------|
| `app.py` | 主重构 + Bug 修复 |
| `TODO-后续优化.md` | 新增 K-N 项（kedou.life 启发）+ O 项（抖音/快手） |
| `docs/ARCHITECTURE.md` | 新建，完整架构文档 |
| `docs/kedou_life_analysis.md` | 新建，竞品分析 |

### 不涉及的文件

- `static/index.html` — 前端未动
- `data/settings.json` — 配置格式未变
- 所有下载路由、PDF 导出、Settings API — 未变

---

## 七、后续优化方向

从 kedou.life 竞品分析和本次实测中识别出的优化项（详见 `TODO-后续优化.md`）：

| 优先级 | 项目 | 说明 |
|:--:|------|------|
| ⭐⭐⭐ | K 字幕提取 | yt-dlp 原生支持，可跳过下载+转录，质量更高 |
| ⭐⭐⭐ | L 元数据增强 | 封面/时长/清晰度展示 |
| ⭐⭐ | O 抖音/快手解析器 | 需要调研专用开源工具 |
| ⭐⭐ | E 批量处理 | 多链接队列 |
| ⭐ | A/D/G/M/N 等 | 搜索前端、暗色模式、桌面端完善等 |

---

## v2.5 补充更新（2026-07-14）

基于用户反馈，在 v2.4 基础上追加以下优化：

### 新增功能

| # | 功能 | Commit |
|---|------|--------|
| 视频解析器独立页面 | `static/parser.html` 全新 UI，首页切为解析器 | `b4032d9` |
| 流式视频下载 | 用户点下载才拉取，不点不占磁盘 | `b4032d9` |
| 平台自动识别 | 后端 `detect_platform()`，前端不显示平台选择器 | `b4032d9` |
| 解析后自动暂停 | `stop_at=transcribe`，转录完成即停，不生成笔记 | `b4032d9` |
| 封面图片代理 | `/api/proxy-image` 绕过 B站防盗链 | `3941bae` |
| 小红书封面提取 | `imageList[0].urlDefault` | `b9c4e42` |
| 小红书时长提取 | `capa.duration`（ffprobe 验证一致） | `f66ae3c` |
| 元数据卡片优化 | 去 emoji 图标、去点赞、加简介、加原视频链接 | `13bc27f` `cbdcc89` |
| 解析历史管理 | 查看、删除、重新解析 | `b3853c4` `3cdf65c` |
| 三种错误态 | 非链接（红）、未知平台（橙）、正常（绿） | `8d6a974` |
| URL 智能提取 | 支持从小红书分享文案自动提取链接 | `be5196f` |
| 加载动画简化 | 只显示「解析中...」+ 弹跳点，无图标流程文字 | `ac12ae4` |

### Bug 修复

| # | 问题 | 修复 | Commit |
|---|------|------|--------|
| 1 | 环境变量与 Claude Code 冲突 | `ANTHROPIC_*` → `VTN_*` | `17d8a1c` |
| 2 | 兜底函数文案过时 | "Groq Whisper large-v3" → "本地 Whisper tiny" | `17d8a1c` |
| 3 | yt-dlp 简介显示 "-" | 空值和 "-" → 空字符串 | `17d8a1c` |
| 4 | 平台 badge 等全流程结束才显示 | `step_resolve` done 事件带上 `platform` | `9471f25` |
| 5 | B站封面无法加载 | 图片代理绕过防盗链 | `3941bae` |
| 6 | 简介区过长无法滚动 | `overflow-y: auto` + 细条滚动条 | `ac12ae4` 后续 |
| 7 | 错误文案不准确 | "无法访问该笔记" → "无法访问该链接" | `be5196f` |
| 8 | 历史下载报 404 | 历史查看隐藏下载，新增重新解析 | `3cdf65c` |

### 破坏性变更

- 环境变量改名（`ANTHROPIC_*` → `VTN_*`），需更新启动配置
- 根路由 `/` 从完整笔记生成页切为解析器页面，原 UI 移到 `/v1`
- 笔记生成功能暂不可用（等待后续大改），解析器独立运行
