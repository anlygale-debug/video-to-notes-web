# Hermes agent 17分钟完整教程分享 — 课后笔记

> 视频作者：小陈同学c_z | 平台：xhs | ❤️ 898
> 转录：本地 Whisper（详细模式 · 1 段并行处理）

---

## 快速安装与初步设置

### 一键安装命令与前置依赖
- 项目官网提供简单的一键安装脚本，支持 macOS 和 WSL2，**不支持裸机 Windows**，需先安装 WSL2。
- 唯一硬性依赖是 **Git**，需提前安装。其他如 Node、Python 等依赖会自动检测并安装。
- 若已安装 Open Claude、Claude Code 或 Cursor 等 AI 工具，可直接将安装地址交给这些工具，让它们代为安装和配置，出错还能自动排查。

> “唯一的一个以来条件,就需要先安装一下 Gate. 其他的依赖比较像 DOS PESANTON 它会自动检查版本并且自动安装依赖。”

### 终端 UI 的个性化调整
- 安装后通过 `hemis` 进入对话窗口，默认颜色可能与白色背景终端冲突，导致文字看不清。
- 可以通过 `/skins` 查看可选皮肤，再用 `/skins 皮肤名` 切换，如 `Delight` 和 `WM` 适配浅色模式。
- 更改模型昵称可通过对话直接要求，例如将 “Hemis” 改为 “恾”，再通过自然语言要求保留原名和昵称。
- 退出会话时终端会显示回话 ID 与恢复命令，方便继续之前的对话。

> “它有一个比 Open-Claude 好的地方就是它会在中端展示工具的调用进行,让你看清楚它在干啥,进入到什么阶段了。”

## 配置 Web UI 管理面板与对话界面

### 官方 Dashboard
- 命令 `hemis dashboard` 启动管理面板，自动打开本地地址。面板可查看版本、会话记录、Token 用量、日志、技能和配置等，支持中文切换。

### 第三方 Web UI
- 克隆第三方开源 Web UI 项目，通过 Hemis 直接运行，即可获得美观的对话页面。
- **推荐设置**：
  - 在外部 UI 设置中将发送快捷键改为 `Ctrl+Enter`（避免中文输入时误发）。
  - 勾选通知声音、显示 Token 数量、显示系统提示等。
  - 可切换主题、选择工作区路径（默认非 Hemis 目录，需手动指定）。
  - 模型选择可检测本地其他 AI 端点（如 Cursor）。

> “点击左下角的外部 UI…… 强烈建议改成 Ctrl+Enter 来发送,否则在输入中文时按回车确认选词就会直接发送出去。”

- 特点：对话自动显示思考过程，Token 用量达 50% 时**自动压缩上下文**，优于 Open Claude。

## 社交渠道接入（以微信为例）

- 支持 Telegram、飞书、微信等渠道，可通过终端命令 `hemis gateways thatab` 交互式选择并配置。
- 微信接入流程：
  1. 选择微信渠道，确认开启 QR 码登录。
  2. 用手机扫描终端输出的 QR 码并在微信中确认。
  3. 在第一句对话中获取配对码，复制到终端执行配对命令（注意删除命令两端可能出现的引号）。
  4. 配对成功后即可在微信中对话，回复支持 Markdown 渲染。

> “那它的回复也是支持 Markdown 渲染的,让它写一篇笔记到 Obsidian 中…… 它看到我 Obsidian 知识库里面有关于 AI 输出内容的要求,所以它了解了这个新鲜后移动了文件的位置。”

- 写入 Obsidian 的笔记内容详细，包含原理、不足和功能特性，说明它使用了 Web Search 内置工具和 Obsidian 的 SQL 检索。

## 核心文件与上下文管理机制

### 文件结构对比 Open Claude
- Open Claude 的记忆文件非常多且细分（如 `System.md`, `User.md`, `Rules.md`, `Memory.md` 等），导致每次对话加载大量上下文，容易遗忘或混乱。
- Hemis 更精简，将 `System.md`, `User.md`, `Memory.md` 合并放入 `Memory/` 目录，并且技术性地控制容量。

> “Hemis 的发生者更精简,他也有 System.md、User.md 和 Memory.md 放在 Memory 里面…… 但是他的技术有容量上限的,上限是 3,275 个字符。”

- **上下文管理机制**：
  - 总字符上限 **2024**（推测为 200k+ 总 token 上下文），超出后自动精简。
  - **1375 字符**为用户层面的记忆上限，每 10 次对话会强制重新生成一次记忆总结。
  - 好处：记忆不会无限膨胀，系统会不断更新、越来越了解你，且不浪费 token。
  - 所有历史对话通过 **存储 + 搜索** 方式找回，在管理面板可查看每次对话的详细记录和工具调用，不会像 Open Claude 那样丢失重要会话。

## Skills 与自动进化能力

### 内置工具与 Skills 规模
- 启动时即展示 **27 个工具和 79 个 Skills**，开箱即用，无需额外安装。

### Skills 的安装与通用性
- 官方 Skills 网页分类清晰（如编程、研究、游戏），点击卡片可获得一键安装指令。
- **所有 Agent 的 Skills 通用**，可将 Claude Code 的 Skills 直接用于 Hemis，安装方式推荐通过自然语言对话完成。
- 可使用 `npx` 安装全局 Skills，供机器上所有 AI 工具共享。

### 自动计划与自主生成 Skills（核心亮点）
- **什么是自动计划**：Hemis 能根据需求自主创建 Skills，并在使用中自我改进、周期性自我提醒、持续加深用户模型理解、行为基准测试与优化。

> “第一就是可以自主创建 Skills。当它发现一个复杂任务并且之前没有现成 Skills 用的时候,它就会把本次经验自动封装成一个 Skills,下次再执行类似任务就能够开箱即用。”

- **实际演示**：要求监控 GitHub 仓库的新 PR，翻译后写入 Obsidian。
  - 使用了 Plan 模式，先生成方案并确认，再编写并测试脚本。
  - 首次执行后识别出“新场景”，提示需要创建新 Skill。
  - 发现未遵守中文输出约定后，自动提出翻译方案，调用大模型 API 集成到脚本中，并自我验证 token 消耗。
  - 最终生成了两个产物：可用的 Python 脚本和对应的 Skill 描述文件（包含使用场景、遇坑与解决办法、注意事项）。

## 从 Open Claude 迁移到 Hemis

- **一键迁移命令**：`hemis clonerepos`，可迁移所有 Skills 并整合 `System.md`、`Memory` 等核心文件。
- 迁移前建议加上参数预览哪些可迁移，需要包含 API key 时可追加 `--password` 参数。
- 迁移非简单复制粘贴，而是**语义理解后重新解析整合**。

## Open Claude vs Hemis 个人看法

- Open Claude 近期版本更新常引入新问题，花费大量精力处理 Bug，老问题如网络断开、规则不生效、上下文积压等频发。
- Hemis 逻辑不同，记忆文件极简，其余靠数据库保存，**能自主创建 Skill 并根据效果自我调试迭代**，上下文积压后仍能通过搜索找回原始对话。
- 安全性方面：Hemis 会检查高风险操作并需人工审批，支持 Docker、SSH 等，权限管理更可靠，内存占用也较小。

> “如果 Open Claude 用得很顺手,没必要迁移。但如果你也遇到和我一样的很多问题,那其实可以尝试一下 Hemis,说不定它能给你一些惊喜。”