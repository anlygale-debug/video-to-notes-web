# Git+Github核心概念大串讲，从零到一全攻略，详细实战教程 — 课后笔记

> 视频作者：技术爬爬虾 | 平台：bilibili | ❤️ 0
> 转录：本地 Whisper（详细模式 · 3 段并行处理）

---

## 一、开篇概述  
本部分为系列视频第一部分，核心目标是把 Git 和 GitHub 在 AI 开发场景下必须理解的概念一次性讲清。不再死记命令，而是掌握核心思想后，直接用自然语言指挥 AI 完成各种版本控制操作。

> “Git 与 GitHub 已经成为了 ASDai 必须必会的基本功之一了，很多 ASDai 的核心功能都是直接围绕着 Git 来运行的。”

## 二、Git 与 GitHub 是什么  
### 2.1 Git：版本控制工具  
- **名称拆解**：Git + Hub。先看左半边 Git。  
- **功能定位**：开源免费的**版本控制**软件。  
> “Git 是一个开源免费的软件，它的功能是版本控制。”  
- **原始版本控制**：像写论文时保存“第一版、第二版、最终版、打死不改最终版……”，这就是最原始的手工版本控制。当项目文件成千上万、多人协作时，手工方式无法承担。  
> “我们小心翼翼保存好论文的每个版本，就是为了应对各种突发情况……这个保存多个历史版本的操作，就是最原始的版本控制。”  
- **Git 的方案**：用**提交（Commit）** 作为版本控制的基本单元，每次提交都会保存仓库此刻的“快照”，所有文件状态被记录，随着提交增多形成一条**提交历史链**，整个仓库随时可回溯、可查看历史。  
> “每完成一次 Commit，Git 保存了一个仓库此时状态的快照，文件夹里所有文件的状态都被记录了下来。”  

### 2.2 仓库的两种类型  
- **本地仓库（Local Repository）**：运行在自己电脑上的 Git 仓库。  
- **远程仓库（Remote Repository）**：把本地仓库上传到服务器，用于备份、分享、协作。  
> “如果你想把本地仓库在远端服务器上进行一个备份，或者想跟其他人分享你的代码，就可以把本地仓库上传到服务器上面，这样就拥有了远端仓库。”  

### 2.3 GitHub：全球最大代码托管与协作平台  
- **名称后半 “Hub”**：中心、汇聚点。全世界开发者把用 Git 管理的仓库上传到 GitHub 网站，这个网站就成了 Git 仓库的枢纽。  
> “GitHub 就是 Git + Hub，它是全球最大的代码仓库托管与协作平台，可以在里面存储、分享并且与他人一起编写代码。”  
- **仓库可见性**：  
  - **公开仓库**：可以被搜索、直接浏览学习代码，代表开源精神。  
  - **私有仓库**：仅自己或授权者可见。  
- **开源项目例子**：Linux 操作系统、 CPython 解释器、Nginx 等。  
> “开源是计算机科学的基石与瑰宝。”

## 三、准备工作：安装与环境配置  
1. **安装 Git**：  
   - Windows：官网下载安装包，按 CPU 架构选择（通常 x86_64），一路下一步。  
   - Mac：在终端执行 `xcode-select --install` 安装命令行工具（包含 Git）。完成后用 `git --version` 验证。  

2. **安装 VS Code**：官网下载对应系统安装包，一路下一步。  

3. **注册 GitHub 账号**：访问 GitHub，如遇网络问题可参考作者前序视频。右上角 Sign up，填写用户名、邮箱、密码，完成邮箱验证后登录。  

4. **配置 AI 辅助工具**：一般使用 **Codex** 或 **Rocul**（文中以 Codex 为例），完成授权绑定。  

5. **AI 一键完成 Git 与 GitHub 绑定**：  
   - 在 Codex 中打开一个空文件夹，输入自然语言提示：“把这个文件夹初始化成 Git 项目，并且推送到 GitHub 上面”。  
   - 按提示提供远程仓库地址（先在 GitHub 新建一个 Repository，复制地址给 AI）。  
   - 授权登录 GitHub 后，任务自动完成。  

## 四、Git 核心概念详解  
### 4.1 初始化（Init）与 .gitignore  
- **Initialize**：让一个文件夹被 Git 管理，成为 Git 仓库。  
  - 在 VS Code 中打开文件夹 → 左侧 Source Control → 点击 “Initialize Repository”。  
  - 初始化后文件夹内生成一个隐藏的 `.git` 子文件夹，存放全部版本控制数据。删除 `.git` 即取消 Git 管理。  
  - **让 AI 操作时可以说**：“把这个目录进行 Git 初始化，注意把不需要的内容都排除掉。”  

- **.gitignore 文件**：  
  - 用于声明**哪些文件不应该被 Git 管理**（如 `.env`、`node_modules` 等）。  
  > “.gitignore 是一个文件，里面声明了仓库里面哪些文件不应该受到 Git 的管理……这些文件应该安全保存在本地，不能上传到 GitHub。”  
  - 语法示例：  
    - 忽略某类文件：`*.log`  
    - 忽略某个目录：`node_modules/` （注意尾部斜杠表示目录）  
  - 最佳实践：在项目初始化时同时创建好 `.gitignore`，从源头防止误提交敏感或多余内容。  

### 4.2 提交（Commit）：版本快照  
- **定义**：每执行一次 Commit，Git 就保存当前全部文件的快照，形成历史记录链。  
> “Commit（提交）是版本控制的基本单元。每完成一次 Commit，Git 就保存了仓库此时状态的快照。”  

- **实际操作**：  
  - 修改或新建文件后，在 VS Code 左侧 Source Control 中会显示待提交文件。  
  - 填写 **Commit Message**（提交信息），用简洁语言说明做了什么。  
  - 点击 Commit 完成。  
- **查看历史**：在 Graph 区域看到提交链，点击某次提交可展开具体文件改动。  
- **放弃未提交修改**：使用 `Discard Changes`（丢弃更改），让文件回到上次提交的状态。  
  > “如果发现 AI 的修改不符合预期，可以随时点击 Discard 按钮，放弃掉 AI 的修改，然后修改提示词让 AI 重做。”  

- **Commit ID（哈希）**：  
  - 每次提交都有一个由哈希算法生成的唯一 ID（Commit Hash）。  
  - 有长 ID 和短 ID 两种形式，功能完全等价，短 ID 更方便交流。  
  > “Commit ID 在一个仓库里面是绝对唯一的，所以跟 AI 交流的时候最好直接使用 Commit ID，这样会更加精确。”  

### 4.3 回退操作的三种方式（借助 AI 自然语言）  
> “在 AI 时代我们完全不需要记忆这些命令，你只需要了解 Commit 的基础概念，就能让 AI 完成一切操作。”

- **1. Discard（放弃未提交更改）**  
  - 行为：撤销尚未 Commit 的改动。  
  - 场景：文件修改后还没提交，直接丢弃。  

- **2. Reset（硬回退）**  
  - 行为：把仓库强制回到某个历史状态，该状态之后的所有提交会被删除。  
  - 示例：让 AI “把仓库还原到这次提交的状态，它后面的所有提交都不要了”。  
  - 风险：若后续提交已推送到远程，需要强制推送覆盖，**多人协作中禁止使用**，易丢失代码。  

- **3. Revert（安全回退）**  
  - 行为：生成一个“反向提交”来抵消某次错误提交，保留原有历史不变。  
  - 示例：只想撤销“香蕉改菠萝”这一次提交，而不影响其他提交，让 AI “把这一次提交撤销掉，保持其他的不变”。  
  - 优势：**安全**，适合多人协作分支，优先使用。  

- **实际让 AI 工作的提示模式**：复制对应 Commit ID，用自然语言说明需求，AI 会在底层执行 `git reset` 或 `git revert` 等命令。  

> “言者所以在意，得意而忘言。……在 AI 时代，我们只需要理解 Git 操作的本意与原理即可，对具体的命令并不需要死记硬背。”

### 4.4 分支（Branch）  
- **定义**：分支是仓库的不同开发线，默认有 `main` 或 `master` 主分支。  
- **原理**：  
  - 创建分支本质是“指针”操作，不拷贝代码。新分支刚创建时与主分支代码完全相同。  
  - 在分支上修改并提交后，更改只属于该分支，切换回主分支时看不到这些修改。  
- **价值**：  
  > “分支对于多人或者多 AI 的协作非常有用，每个人或者每个 AI 都可以创建出自己的分支，在分支上进行功能开发，而不会影响到其他人。”  
- **合并（Merge）**：功能开发完成后，把分支合并回主分支。  
- **操作提示**：在 VS Code 左下角显示当前分支，点击后可创建基于当前分支的新分支。  

---

通过本部分的学习，建好本地仓库、理解 Commit 和分支，并借助 AI 用自然语言执行复杂 Git 操作，为后续远程仓库与多人协作打下基础。
## 分支的创建与基本操作

在 Git 中，分支是多人协作或多人阵开发的核心机制。你可以基于当前主干或任意提交创建新分支，分支上的修改互不影响。

- **创建分支**：基于当前 `master` 分支创建新分支（例如 `feature`），新分支初始代码与主干完全一致。
- **在分支上开发**：修改代码并提交，这些提交只属于当前分支。
- **切换分支**：切换回 `master` 分支后，看不见 `feature` 分支上的改动。

> 當你對 FETER（feature）進行代碼修改並且完成 Comet（commit），這些新的提交只對 FETER 分支有效，只能在 FETER 被看到，而不會影響主幹。換句話說，當你切換回主幹的時候，是看不到 FETER 分支的代碼修改的。

- **合并（Merge）**：将分支上的工作成果合并回主干的操作称为 Merge。合并后，分支的改动就会出现在主干上。
- **删除分支**：合并完成后一般会删除已用过的分支。需要注意，删除分支前必须先切换到其他分支（不允许删除当前所在分支）。

### 基于历史提交创建分支

除了基于最新提交，还可以基于任意一个历史提交创建分支。右击某次提交选择 `Create Branch`，仓库文件会回退到该次提交对应的状态。

> 基於任何一次歷史提交創建分支……倉庫的文件也還原到了那次提交對應的狀態。

这种方式与“分离头指针”（Detached HEAD）相比更安全：有了分支管理后可以自由修改代码而不容易丢失。

### 基于已有分支创建分支

你也可以基于任意一个已有分支创建新分支。例如基于 `feature` 分支创建 `feature2`，后者会包含 `feature` 上已有的全部代码。如果基于 `master` 创建 `feature3`，则 `feature3` 只能看到 `master` 分支的代码。

## HEAD 的概念

HEAD 可以理解为“当前仓库处于哪一次 commit 上”的指针。

- 当切换到某分支时，HEAD 指向该分支的最新一次提交。
- 当处于“分离头指针”（Detached HEAD）状态时，HEAD 直接指向某一次历史提交，而不指向任何分支。  
> 在分離頭指針的狀態下，最好不要進行修改，容易搞丟代碼。

## Worktree（工作树）

Worktree 本质上是利用 Git 创建一个新分支，并把该分支的代码复制到一个新的文件夹中。主文件夹和分支文件夹可以并行工作，互不干扰。底层通过 Git 关联，分支文件夹的改动可以轻松合并回主干。

- **创建 Worktree**：在项目文件夹中执行 `code --worktree <分支名>`，会在新目录下打开一个新的工作环境。
- **并行开发**：可以在 Worktree 中独立完成开发、测试等操作，不影响主干文件夹。
- **完成开发后**：提交并合并回主干，最后删除 Worktree。

> 在工作宿上完成了開發以後，就可以讓 AI 把工作宿合併回主幹……主幹分支和分支文件夾可以並行工作，互相不干擾。

使用 Worktree 可以在不同窗口同时进行多个任务的开发，显著提升效率。

## 合并冲突（Merge Conflict）

当两个分支修改了同一个文件的同一行代码时，Git 无法自动决定保留哪个改动，就会产生合并冲突。

- **冲突表现**：合并过程会中断，需要人工决定最终保留的内容。
- **解决冲突**：可以选择保留其中一个分支的改动，也可以两个都保留，并指定先后顺序。

> 如果兩個分支修改了同一個文件的同一行代碼，合併的時候 Git 就無法自動確認到底保留哪個改動，這個場景就是 Merge Conflict……解決衝突在 Git 裡面其實是個比較繁瑣的操作，不過現在有了 AI，一切就簡單太多了。

使用 AI 工具时，冲突出现后 AI 会给出多个选项，按指导选择即可完成冲突解决。

## Git 的分区模型

Git 中包含四个关键分区，所有 Git 操作本质都是它们之间的文件同步：

1. **工作区（Working Directory）**：电脑磁盘上的本地文件夹。
2. **暂存区（Staging Area / Index）**：提交之前的“检查点”，可以先选择要提交的修改。
3. **本地仓库（Local Repository）**：commit 后文件改动的存放处。
4. **远程仓库（Remote Repository）**：如 GitHub、GitLab 等，用于分享和备份代码。只有推送到远程仓库的代码才能被他人看到。

常用操作对应的分区流转：

- `git clone`：将远程仓库保存到本地，同时创建本地仓库和工作目录。
- `git add`：将工作区的改动添加到暂存区。
- `git commit`：将暂存区的所有改动提交到本地仓库。
- `git push`：将本地仓库的改动推送到远程仓库。
- `git pull`：将远程仓库的最新改动更新并合并到本地（相当于 `git fetch` + `git merge`）。

> 在 VS Code 裡點擊 Commit 按鈕的時候也同時執行了 `git add` 的命令……這樣大幅簡化了 Git 的操作複雜度。

在 VS Code 中也可以手动使用暂存区：只将部分文件添加到暂存区（Stage Changes），则仅提交暂存区中的文件。

## 绑定远程仓库的两种方式

### 方式一：克隆远程仓库到本地（git clone）

1. 在 GitHub 上创建新仓库，获得仓库地址。
2. 在 VS Code 中使用 “Clone Repository”功能，粘贴地址并选择本地文件夹。
3. 克隆完成后，本地自动拥有本地仓库和工作目录。
4. 在本地修改代码、commit 后，通过 “Publish” / `git push` 将本地 commit 推送到远程。

> 使用 `git push` 的時候，只有你是倉庫管理員，或者管理員給你添加了權限，你才可以 push 成功，不能往別人的倉庫裡面直接 push 代碼。

### 方式二：先初始化本地仓库，再推送到远程

- 用 `git init` 将本地文件夹初始化为仓库。
- 进行一次初始 commit。
- 在 VS Code 中选择 “Publish Branch”，填写远程仓库名，并选择公开或私有。
- 推送成功后，远程仓库中即可看到代码。

## 从远程拉取更新（git pull / fetch + merge）

当远程仓库有新的提交时，本地需要通过拉取操作同步。

- 若直接在远程仓库进行了更改（例如通过网页新建文件并 commit），本地点击 “Sync Changes” 即可将远程提交拉取到本地。
- 在多人的协作场景下，同事推送新提交到远程后，通常也应先用 `git pull` 拉取并合并最新的远程改动。

> 除了直接在遠端倉庫修改代碼，還有一種更常見的情況是同事或者協作者把他自己的提交先上傳到遠端倉庫，這時候我們一般先使用 `git pull` 把遠端倉庫的改動拉取到本地。

## GitHub 仓库界面核心要素

仓库地址遵循固定的命名规则：  
`github.com / 開發者名字 / 倉庫名字`

- **代码区**：展示项目全部源码，可在线查看或下载 ZIP 包，也可获取克隆地址。
- **文件信息**：每个文件后面显示最后一次 commit 信息及更新日期（据此可判断项目是否长期未维护）。
- **README**：项目说明，展示在代码区下方，通常包含项目用途、解决的问题及运行方法。
- **Releases**：发布版本信息，通常包含打包好的软件和更新内容，可按需选择下载。
- **About**：项目简介、标签、Star 数、开源协议等。
- **Star（加星）**：类似点赞与收藏，能反映项目的热门程度。如果对项目感兴趣可以给作者点 Star。
- **Fork（复刻）**：将项目保存一份到自己的名下，便于深入学习源码、按需修改，甚至通过 Pull Request 将修改贡献回原项目。
- **Issues**：与项目作者讨论的空间，可发起新 Issue 报告 Bug、提出功能建议或帮助其他人解决问题。Issue 分为 Open（未解决/讨论中）和 Closed（已解决）。

> Fork 可以幫助我們更深入地學習源代碼，可以根據自己的需求對源代碼進行 DIY，還可以嘗試把自己的修改通過 Pull Request 合併進作者的主代碼庫，為開源項目添加代碼。
## 给项目做贡献：Fork 工作流

> “这个账号是P88下的小根班……小根班想为这个开源项目贡献自己的代码，这里小根班是没有权限直接向这个项目本身提交代码的，所以他要做的第一步就是点击这里的 Fork。”

### Fork 的含义
- Fork 就是**把原项目复制一份到自己的 GitHub 名下**。这个复制出来的仓库称为“子项目”。
- Fork 之后，在自己的子项目上进行任何修改都不会影响原来的项目（上游项目/母项目）。

### 贡献流程（Fork 工作流）
1. **Fork 项目**  
   在原项目页面点击 `Fork` → `Create fork`。
2. **克隆到本地**  
   复制自己名下子项目的地址，用 VS Code 等工具克隆下来。
3. **创建分支（Branch）**  
   > “小根班在动手修改任何代码之前，一定要先创建分支，不要直接在主分支上修改。”

   创建分支最大的好处：当从上游（母项目）同步代码时，因为改动都在分支上，同步不会产生冲突。
4. **在分支上修改并提交**  
   完成代码改动，本地 `commit`，然后推送到 GitHub（`push` 到自己的子项目）。
5. **创建 Pull Request（合并请求）**  
   在子项目页面点击 `Pull request` → `New pull request`。
   - 一定要注意**合并方向**：把子项目的功能分支合并到母项目的主分支。
   - 填写标题说明（例如“水果清单增加更多水果”）。
   > “Pull request 的直译过来是拉取请求，不过更喜欢它的另外一个翻译，那就是合并请求。合并请求是把一个分支的改动合并进另一个分支的提案。”
6. **代码审核（Code Review）**  
   母项目的管理员会看到 Pull Request。管理员可以：
   - 查看 `Files changed` 中的差异；
   - 提出修改建议；
   - 如果没问题，点击 `Merge pull request` → `Confirm merge`。
7. **完成合并**  
   合并后，贡献者的代码会进入母项目主分支，贡献者头像也会出现在仓库的 `Contributors` 中。

### 同步上游代码（避免冲突）
> “小根班在进行 Pull request 之前，他有一个准备工作必须要做：就是先把母项目那边的最新代码同步过来，确保自己这边已经包含了母项目那边最新的代码，并且没有冲突，这时候才能创建 Pull request。”

- 可以用 AI 辅助完成：告诉 AI 把上游最新的改动合并进当前功能分支，冲突时让自己选择处理。

---

## 直接协作者模式（Collaborator）

- 当开发者和管理员在同一公司，或管理员把开发者添加为 **Collaborator**（项目合作者）时：
  - 管理员在仓库 `Settings` → `Collaborators` → `Add people` 添加对方。
  - 被邀请者在 GitHub 通知中确认邀请。
  - 这样开发者就有权限**直接推送代码到原项目的远程仓库**，不再需要 Fork。
- 流程简化为：拉分支 → 修改提交 → 推送分支 → 创建 Pull Request → 审核合并。

---

## Cherry-pick（拣选提交）

> “Cherry-pick 字面意思是挑选樱桃，在 Git 的语境里面就是拣选提交。”

- 作用：**只把某几次提交合并到另一个分支，而不全部合并**。
- 例如：
  - 在功能分支上进行了三次提交（蔬菜清单、肉类清单、主食清单）。
  - 只把蔬菜和主食的提交合并到主分支，跳过肉类。
  - 操作：复制需要的提交 ID，使用 `cherry-pick` 将它们应用到目标分支。
- 结果：只有被拣选的提交会出现在主分支历史中，其他提交不受影响。

---

## Stash（暂存）

> “Stash 是临时存储……如果我在这里直接切换分支，那这里写到一半的代码会被直接带过去，这显然不是我想要的。”

- **使用场景**：当前分支有未写完的代码（尚未提交），需要紧急切换到其他分支处理临时任务。
- 两种解决方案：
  1. 在当前分支直接 `commit`（提交），再切换。
  2. **使用 Stash**：将未完成的修改临时存储起来。
     - 操作：点击三个点 → `Stash` → 起个名字（或直接回车）。
     - 工作区恢复干净，可以安全切换分支处理临时任务。
     - 回来时，选择 `Pop` 将暂存的代码恢复并继续编写。

---

## Rebase（变基）

> “Rebase 意思是变基……从最终达成的效果来看，可以认为是把 master 合并进了 feature 分支；第二层理解是把 feature 分支变基到了 master。”

- Rebase 的效果：
  - 把当前分支的“根基”从原来的点，**变到目标分支的最新提交之后**。
  - 旧提交会被重新生成成新的提交（Commit ID 改变）。
  - 历史记录会变成一条直线，**不会产生额外的 `Merge` 提交记录**，更加整洁。
- 操作示例：
  - feature 分支有 C2 提交，master 有 C3、C4、C5。
  - 在 feature 分支上执行 `rebase master`。
  - 结果：C2 变为 C2'，出现在 C5 之后，整个历史变成一条直线。
- **重要提醒**：Rebase 之后，**必须强制推送**（`git push --force`）到远程仓库。

> “强制推送只适合于这种只有你一个人使用的分支。如果这个分支是多人协作分支……是万万不能使用强制推送的，你使用强制推送有可能会把别人写的代码给覆盖掉，把别人写的代码搞丢。”

---

## GitHub Issues 与讨论功能

- 仓库左上角的 **Issues** 可以直接与项目作者讨论。
  - 点击 `New issue` 发起讨论。
  - 用途：报告 Bug、提出新功能期待、参与讨论或帮助他人解决问题。
- Issue 状态：
  - **Open**：未解决的 Bug 或仍在讨论的问题。
  - **Closed**：已解决的 Bug 或已结束的讨论。
- 使用建议：在提问前可以 **先搜索 Issue**，可能别人遇到过同样问题并已有解决方案。

---

## GitHub 实用快捷键与功能

| 快捷键 / 操作 | 功能说明 |
|---------------|-----------|
| `s` | 快速打开 GitHub 搜索栏 |
| `t` | 快速定位到文件搜索，可按文件名搜索文件 |
| 打开文件后按 `y` | 定位到具体行号，可选中该行 |
| 选中行后 `Copy permalink` | 复制永久链接，方便分享代码位置 |
| `Blame` 视图 | 查看每行代码由谁、在何时提交 |
| `?` | 打开快捷键速查表 |
| `g c` | 快速转到代码（Go to Code） |
| `g i` | 快速转到 Issues |
| `.`（句号键） | 打开网页版 VS Code，在线查看、搜索、安装插件 |

---

## GitHub Codespaces 在线开发环境

> “键上的句号，GitHub 会为我们打开一个网页版的 VS Code，我们可以在里面查看代码……如果你想对里面的代码进行调试，GitHub 同样提供了一个 CodeSpace 的运行环境。”

- 点击 `Code` → `Codespaces` → `Create codespace`，可申请远程运行环境（例如双核 8GB）。
- 直接在浏览器中调试、修改代码，点击运行按钮即可看到输出。

---

## 操作安全与协作意识总结

- **永远不要在 main/master 上直接修改**，始终在功能分支工作。
- 发起 Pull Request 前**先同步上游最新代码**，在本地解决冲突再推送。
- **Rebase 使历史整洁，但只限个人分支使用**；多人共用分支禁用强制推送。
- **Cherry-pick** 用于按需选择提交合并；**Stash** 用于暂存未完成的工作，方便切换上下文。